{
  "name": "tumbatumba-site",
  "version": "0.0.0",
  "private": true,
  "description": "Repo-root dev tooling (Prettier) for the Aling Nene's Tumba Tumba site. The site itself lives in web/ (Next.js static export).",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm run format:check"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
